name: Docker Image

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: docker-build
    env:
      PACKAGE_REGISTRY: ${{ vars.PACKAGE_REGISTRY }}
      PACKAGE_IMAGE: ${{ vars.PACKAGE_IMAGE }}
      PACKAGE_USER: ${{ secrets.PACKAGE_USER }}
      PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check package configuration
        run: |
          set -euo pipefail

          missing=0
          for name in \
            PACKAGE_REGISTRY \
            PACKAGE_IMAGE \
            PACKAGE_USER \
            PACKAGE_TOKEN
          do
            if [ -z "${!name:-}" ]; then
              echo "Missing required secret: ${name}" >&2
              missing=1
            fi
          done

          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

      - name: Build image
        env:
          IMAGE_TAG: sha-${{ github.sha }}
        run: |
          set -euo pipefail
          REF_TAG=$(printf '%s' "$GITHUB_REF_NAME" \
            | tr '[:upper:]' '[:lower:]' \
            | tr '/:' '--' \
            | tr -c 'a-z0-9_.-' '-' \
            | sed 's/^-*//; s/-*$//; s/^$/ref/')

          docker build \
            -t "$PACKAGE_IMAGE:$IMAGE_TAG" \
            -t "$PACKAGE_IMAGE:$REF_TAG" \
            .

          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            docker tag "$PACKAGE_IMAGE:$IMAGE_TAG" "$PACKAGE_IMAGE:latest"
          fi

      - name: Login to Gitea container registry
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          printf '%s\n' "$PACKAGE_TOKEN" | docker login "$PACKAGE_REGISTRY" \
            --username "$PACKAGE_USER" \
            --password-stdin

      - name: Push image
        if: github.event_name != 'pull_request'
        env:
          IMAGE_TAG: sha-${{ github.sha }}
        run: |
          set -euo pipefail
          REF_TAG=$(printf '%s' "$GITHUB_REF_NAME" \
            | tr '[:upper:]' '[:lower:]' \
            | tr '/:' '--' \
            | tr -c 'a-z0-9_.-' '-' \
            | sed 's/^-*//; s/-*$//; s/^$/ref/')

          docker push "$PACKAGE_IMAGE:$IMAGE_TAG"
          docker push "$PACKAGE_IMAGE:$REF_TAG"

          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            docker push "$PACKAGE_IMAGE:latest"
          fi

  deploy:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: deploy
    env:
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER }}
      DEPLOY_PORT: ${{ vars.DEPLOY_PORT }}
      DEPLOY_SCRIPT_PATH: ${{ vars.DEPLOY_SCRIPT_PATH }}
      DEPLOY_SSH_KEY_B64: ${{ secrets.DEPLOY_SSH_KEY_B64 }}
      DEPLOY_SSH_KNOWN_HOSTS: ${{ secrets.DEPLOY_SSH_KNOWN_HOSTS }}
      DEPLOY_IMAGE_TAG: sha-${{ github.sha }}
    steps:
      - name: Check deploy configuration
        run: |
          set -euo pipefail

          missing=0
          for name in \
            DEPLOY_HOST \
            DEPLOY_USER \
            DEPLOY_SSH_KEY_B64 \
            DEPLOY_SSH_KNOWN_HOSTS
          do
            if [ -z "${!name:-}" ]; then
              echo "Missing required secret: ${name}" >&2
              missing=1
            fi
          done

          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

      - name: Configure SSH
        run: |
          set -euo pipefail

          install -d -m 700 "$HOME/.ssh"
          printf '%s' "$DEPLOY_SSH_KEY_B64" | base64 -d > "$HOME/.ssh/deploy_key"
          chmod 600 "$HOME/.ssh/deploy_key"
          printf '%s\n' "$DEPLOY_SSH_KNOWN_HOSTS" > "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/known_hosts"

      - name: Deploy over SSH
        run: |
          set -euo pipefail

          DEPLOY_PORT="${DEPLOY_PORT:-22}"
          DEPLOY_SCRIPT_PATH="${DEPLOY_SCRIPT_PATH:-/usr/local/sbin/deploy-crm}"

          ssh -i "$HOME/.ssh/deploy_key" \
            -p "$DEPLOY_PORT" \
            -o IdentitiesOnly=yes \
            -o StrictHostKeyChecking=yes \
            "$DEPLOY_USER@$DEPLOY_HOST" \
            bash -se -- \
            "$DEPLOY_SCRIPT_PATH" \
            "$DEPLOY_IMAGE_TAG" <<'REMOTE'
          set -euo pipefail

          DEPLOY_SCRIPT_PATH="$1"
          DEPLOY_IMAGE_TAG="$2"

          sudo "$DEPLOY_SCRIPT_PATH" "$DEPLOY_IMAGE_TAG"
          REMOTE
